// Tailwind CSS v4 runs through its PostCSS plugin; the authored stylesheet is
// imported by app/globals.css.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
